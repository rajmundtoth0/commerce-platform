# uat — auto-synced from main. Cluster to be provisioned later.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: commerce-platform-uat
  namespace: argocd
  finalizers: [resources-finalizer.argocd.argoproj.io]
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/commerce-platform.git
    targetRevision: main
    path: deploy/helm/commerce-platform
    helm:
      releaseName: commerce
      valueFiles:
        - values.yaml
        - values-uat.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: commerce-uat
  syncPolicy:
    automated: {prune: true, selfHeal: true}
    syncOptions: [CreateNamespace=true, ApplyOutOfSyncOnly=true]
    retry:
      limit: 3
      backoff: {duration: 10s, factor: 2, maxDuration: 2m}
